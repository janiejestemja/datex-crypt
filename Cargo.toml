[package]
name = "datex-crypt"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
openssl = "0.10"
tokio = {version = "1.47.1", features = ["full"]}
zeroize = "1.8.1"

[features]
client = []
server = []
